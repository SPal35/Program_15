import math

numbers = [1.1, 2.2, 3.3, 4.4]

# Calculate the sum using math.fsum
total_fsum = math.fsum(numbers)
print("Sum using fsum:", total_fsum)
numbers = [1, 2, 3, 4]

# Calculate the sum using the built-in sum function
total_sum = sum(numbers)
print("Sum using sum:", total_sum)
